<%= form_for(@taco) do |f| %>
  <% if @taco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taco.errors.count, "error") %> prohibited this taco from being saved:</h2>

      <ul>
      <% @taco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="field">
    <%= f.label :meat_id %><br>
    <%= f.number_field :meat_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
